Files mounted on the filesystem that represent hardware devices, typically found in /dev/. For example:

```
/dev/sda1 # Disk partition
/dev/null # A file that discards data
/dev/tty  # A terminal
```

