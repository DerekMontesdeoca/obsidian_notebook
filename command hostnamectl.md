A command to configure the hostname via [[systemd]]. 

Print infomation with hostnamectl status. You can also use `hostnamectl hostname` to get the hostname only.

To set the hostname, do `hostnamectl hostname [name]`. 

